import React from 'react';
import PropTypes from 'prop-types';

const TestPage = props => {
  return (
    <div>
      Testing
    </div>
  );
};

TestPage.propTypes = {

};

export default TestPage;
